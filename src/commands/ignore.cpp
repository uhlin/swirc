#include "common.h"
#include "ignore.h"

/*
 * usage: /ignore [regex]
 */
void
cmd_ignore(const char *data)
{
    (void) data;
}

/*
 * usage: /unignore [#]
 */
void
cmd_unignore(const char *data)
{
    (void) data;
}
